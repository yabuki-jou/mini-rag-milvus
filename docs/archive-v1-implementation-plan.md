# 智慧档案与企业文档智能 V1 实施计划

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 业务方向 | 智慧档案与企业文档智能 |
| V1 项目名称 | 工程项目资料智能归档与检索工作台 |
| 文档类型 | 实施计划（不含实现代码） |
| 依据 | `docs/requirements.md`、`docs/architecture.md`、`docs/database-design.md`、`docs/api-design.md` |
| 验证门槛 | `docs/review/verification-freeze-checklist.md`（v3） |
| 状态 | AV1-P01～P03、P04 前置模型分层、P04.1，以及 AV1-C02 本地迁移验证已完成；当前进入 P04.2；云端部署与完整栈资源验证暂定至 V1 完成后；后续归档业务 API 尚未实现 |
| 更新日期 | 2026-08-08 |

本计划只安排需求编号 `FR-030`～`FR-041` 的智慧档案 V1 工作。它不替代
`docs/implementation-plan.md` 中已经完成的既有 RAG 与 Agent 基座记录，也不把计划中的
模块、接口或测试描述为已经存在。

## 2. 实施原则与边界

### 2.1 执行顺序

```text
智慧档案 V1 实施顺序（当前已完成 P01～P03、P04 前置模型分层与 P04.1）
→ AV1-P01 Parser 可行性验证与规则冻结
→ AV1-P02 虚构验收资料与人工标注
→ AV1-P03 数据库与模型基础
→ AV1-C01 历史 Chroma 独立实验（已完成）
→ AV1-C02 Chroma 向量后端本地迁移（已完成）
→ AV1-P04.2 清单项 API 与派生状态（当前）
→ AV1-P05 上传
→ AV1-P06 正式解析
→ AV1-P07 手工草稿与字段检查
→ AV1-P08 AI 建议
→ AV1-P09 确认入档与 Final Collection
→ AV1-P10 清单关联、目录与审计
→ AV1-P11 正式检索与阈值标定
→ AV1-P12 带证据问答
→ AV1-P13 删除与跨存储恢复
→ AV1-P14 集成验收与文档交付
```

按学习规则，进入编码后一次只执行一个任务；每个任务验证完成并由学习者确认后，才进入
下一任务。`AV1-P02` 与 `AV1-P01` 没有代码依赖，但为保持单步骤学习节奏，先完成
`AV1-P01`，再准备完整验收资料。

### 2.2 不可跨越的门槛

| 门槛 | 对应计划任务 | 在冻结前允许做什么 | 在冻结前不得声称完成什么 |
|---|---|---|---|
| 1.1 Parser 规则 | AV1-P01 | 编写隔离的可行性验证代码与样本 | 正式 Alembic 迁移、解析归档 API、Parser 验收测试 |
| 1.0 验收资料 | AV1-P02 | 继续业务基础实现 | 检索/问答质量验收、阈值标定 |
| C01 Chroma 独立验证 | AV1-C01 | 记录候选云主机的 Chroma 独立实验，不删除 Milvus 数据 | 历史实验可追溯，但不构成部署批准 |
| 2.1 BGE/Chroma | AV1-P09 | 完成 PostgreSQL、手工归档路径和 AV1-C02 | FR-039 正式检索 API、检索验收 |
| 2.2 检索阈值 | AV1-P11 | 实现检索纵向切片并记录候选分数 | 将问答接口视为完成或验收通过 |
| 3.1 跨存储恢复 | AV1-P13 | 实现确认、删除的正常路径 | FR-036、FR-040 的集成验收 |
| 3.2 回答引用 | AV1-P12 | 实现问答切片 | FR-039 问答集成验收 |

门槛的完整定义与冻结记录以
`docs/review/verification-freeze-checklist.md` 为准；本节只说明计划依赖，不能替代该清单。

### 2.3 共同工程约束

- 保持 `Router → Application Service → Domain Service → 存储/外部依赖` 分层；不增加空转 Repository 层。
- 每个受保护接口从真实存在的 `X-User-ID` 建立 `ProjectContext`；客户端和模型不能提交或覆盖所有者、项目/知识库范围。
- PostgreSQL 是业务事实来源，原文件与解析快照位于文件系统，Chroma 只保存可重建的正式 Chunk；
  Compose 内 API 经内部 `chroma:8000` 访问。仅本地开发时可额外映射 `127.0.0.1:8001` 供宿主机
  Python 调试，局域网和公网不可访问。云端是否部署及其端口策略暂定至 V1 完成后。
- 未确认、待重新确认及 `visibility_blocking` 删除中的档案，均不得进入正式目录、检索或问答。
- 普通自动测试不调用真实 DeepSeek；外部模型只使用虚构或脱敏资料。旧 RAG/Agent 测试必须继续通过或明确说明其环境依赖。
- 不实现标书投标、OCR、Excel、跨项目检索、前端、JWT、多角色、混合检索、Rerank、任务队列或知识图谱。

## 3. 任务总览

| ID | 需求 | 依赖 | 目标 | 完成判定 |
|---|---|---|---|---|
| AV1-P01 | FR-033、NFR-016 | 无 | 验证 Parser 的定位、归一化和有效文本规则 | 门槛 1.1 已冻结并写入 Parser 说明 |
| AV1-P02 | NFR-017、质量验收 §12 | 无（建议在 P01 后） | 准备虚构样本、人工标注和固定问题集 | 门槛 1.0 资料与 Ground Truth 可复现 |
| AV1-P03 | FR-030～041 共用 | P01 | 建立 V1 数据库模型、迁移和公共授权/错误基础 | 空库前向迁移与约束测试通过 |
| AV1-P04 | FR-030、FR-031 | P03 | 项目、演示清单、版本控制与所有权 | 项目/清单 API 正常、冲突、越权测试通过 |
| AV1-C01 | NFR-015、历史部署实验 | 无 | 在候选 2 vCPU / 2 GB 环境验证 Chroma 内部网络、重启持久化、隔离、删除与基础资源 | 历史实验已记录，不构成部署批准 |
| AV1-C02 | 既有 RAG 基座、NFR-015 | C01 | 将现有 VectorService 及本地 Compose 从 Milvus 替换为 Chroma，并完成本地迁移验证 | 代码、单测、命名空间、隔离/删除与 Docker 健康验证通过；云端完整栈另行决定 |
| AV1-P05 | FR-032 | P03、P04 | 项目内上传、容量与原文件哈希去重 | 文件保存与重复/上限/隔离测试通过 |
| AV1-P06 | FR-033 | P01、P03、P05 | 将冻结后的 Parser 接入文档状态、快照与重试 | 四格式定位、扫描件失败、幂等测试通过 |
| AV1-P07 | FR-035 | P03、P06 | 人工草稿、字段证据、检查和重新确认入口 | 无 AI 的完整手工确认前置条件可验证 |
| AV1-P08 | FR-034 | P06、P07 | DeepSeek 分类、字段与证据建议及安全重生成 | mock 测试覆盖成功、失败、人工覆盖保护 |
| AV1-P09 | FR-036 | P03、P06、P07 | 确认入档、Final Collection 与可见性闸门 | 门槛 2.1 纵向链路与索引基线已记录 |
| AV1-P10 | FR-037、FR-038、FR-041 | P04、P07、P09 | 人工关联、缺失派生、双列表和脱敏审计 | 关联、筛选、目录隔离、审计 API 测试通过 |
| AV1-P11 | FR-039（检索） | P02、P09 | 正式检索与 `min_relevance_score` 标定 | 门槛 2.2 已冻结，检索验收通过 |
| AV1-P12 | FR-039（问答） | P11 | 仅基于正式证据的 DeepSeek 问答 | 门槛 3.2 引用正确率验收通过 |
| AV1-P13 | FR-040；FR-036/040 恢复 | P05、P09、P10 | 物理删除、可见性阻断与可重入恢复 | 门槛 3.1 故障注入集成测试通过 |
| AV1-P14 | FR-030～041 | P02～P13 | 全链路验收、README/报告同步 | 验收结论、未验证项和演示步骤可追溯 |

## 4. 分任务计划

### AV1-P01 Parser 可行性验证与规则冻结（已完成，2026-08-06）

**目标与范围**

- 验证 PDF 页码、DOCX 段落序号、TXT/MD 行号范围均从 1 开始；DOCX 同时产生规范化段落文本前 50 字的辅助定位。
- 已固定 UTF-8 解码、换行归一化、空白归一化、表格/编号列表处理、有效文本最小字符阈值与最大解析文本量，详见 `docs/archive-v1-parser-design.md`。
- 使用同一原文件重复解析，确认定位和快照哈希可重复生成；扫描 PDF 或空壳文件必须得到 `PARSE_FAILED` 对应的稳定失败分类。

**明确不做**

- 不创建 `projects`、`archive_documents`、`parsed_snapshots` 等正式迁移或模型。
- 不注册归档 Router，不调用 DeepSeek/Chroma，不改写现有 `documents` 业务状态。

**预期改动范围（执行时再确认）**

- `app/services/archive_parser_service.py`（归档专用隔离适配器，未接入旧 RAG Parser）；
- `tests/test_archive_parser_spike.py`（运行时动态生成虚构样本）；
- `docs/archive-v1-parser-design.md`（冻结规则与实际结果）。

**验证与完成条件**

1. 固定样本的 PDF/DOCX/TXT/MD 各至少一份解析成功；定位类型和值满足需求 §7.6。
2. 同一文件重复执行的 `ParsedFragment` 顺序、定位和 `snapshot_hash` 一致。
3. 无可提取文本 PDF 返回扫描件/无文本的受控失败分类，不被误判为成功。
4. 空壳文本被有效文本阈值过滤；阈值和规范化规则写入 Parser 设计说明。
5. 本任务相关测试和 `python -m compileall -q app tests` 通过；冻结结论同步到验证清单引用的位置。

### AV1-P02 虚构验收资料与人工标注（已完成，2026-08-06）

**目标与范围**

- 形成 12～18 份虚构资料，覆盖六个资料类型以及 PDF、DOCX、TXT、MD；增加 2～3 份异常样本。
- 为每份资料标注类型、可识别字段和标准证据；准备 8 个有据、2 个无据、2 个隔离固定问题。
- 样本与标注不含真实商业秘密、个人敏感信息或未获授权资料。

**完成条件**

- 生成器和标注格式受版本控制；生成的本地资料位于 `tests/pytest_docs/` 并由 Git
  忽略，目录缺失时测试自动调用生成器重建。
- 其仅作为质量评测 Ground Truth；不作为真实工程归档规则，也不直接决定模型 Prompt。

**完成结果（AV1-P02，2026-08-06）**

- 已生成 12 份正常虚构资料（六类资料各 2 份，覆盖 PDF、DOCX、TXT、MD）和 3 份
  异常资料（字段缺失/分类混淆、同项目原文件哈希重复、无可提取文本的扫描 PDF）。
- 已生成字段与证据 Ground Truth、12 个固定问题（8 有据、2 无据、2 项目隔离）及本地
  查看说明；`tests/test_archive_v1_evaluation_data.py` 验证资料、证据、重复哈希和问题集契约。
- 本任务未调用 DeepSeek，也未执行 Chroma 检索、阈值标定或问答质量评分；这些结果仍分别由
  AV1-P11、AV1-P12 与 AV1-P14 产生。

### AV1-P03 数据库、模型与公共授权基础（已完成，2026-08-06）

**目标与范围**

- 在 `0004_remove_leave_domain` 后创建前向 `0005_archive_v1_schema`，实现 `projects`、归档文档/快照/字段/证据、清单/关联、操作/审计等已确认结构和约束。
- 依数据库设计将 `documents.content_hash` 演进为原文件 `file_hash`，建立项目关系、容量计数、同项目哈希唯一性和与知识库的一对一范围约束；不把旧文档自动转换为档案。
- 新增 `ProjectContext` 所有权依赖、归档枚举、稳定错误码基础和最小 Schema/Model；保留现有 Agent、聊天和旧知识库行为。

**测试与完成条件**

- 空 PostgreSQL 前向迁移、迁移 SQL 检查和数据库约束测试通过；失败时不使用 `alembic stamp` 掩盖问题。
- 覆盖同用户项目名、同项目 `file_hash`、每文档七字段/当前快照、运行中操作、`visibility_blocking` 的数据库约束。
- 既有迁移与回归测试仍可运行；在线 PostgreSQL 若环境不可用，必须单列为未验证项。

### AV1-P04 项目与项目清单

**共同边界**

- 暂不实现档案—清单关联（AV1-P10）。
- 删除空项目时不级联删除其绑定的 `knowledge_bases` 记录。该记录可能仍在旧
  `/knowledge-bases` 接口中可见；旧系统不感知项目创建/删除生命周期，后续如需清理须由
  独立运维任务处理，不能在项目删除接口中删除可能承载旧文档的数据。

#### AV1-P04.1 项目 CRUD、内部知识库范围与模板复制（已完成，FR-030）

**已完成范围**

- 实现项目创建、读取、修改、空项目删除和 `X-User-ID` 所有权校验。
- 创建项目时可选择复制唯一的五项虚构演示清单；不提供用户自定义模板的创建、保存或复用。

**已验证重点**

- 同用户去首尾空格后的重名冲突、跨用户可同名、越权不泄露、非空项目禁止删除。
- 模板复制互不影响；修改关键匹配字段时，后续关联会失效的规则由 AV1-P10 覆盖。

**完成结果（P04.1 FR-030，2026-08-06）**

- 已实现 `POST/GET/PATCH/DELETE /projects`、项目专属内部知识库范围、同用户去空白重名冲突、
  乐观锁、所有权校验、分页列表与非空项目删除拒绝。
- `use_demo_checklist=true` 会在创建事务中复制五项虚构演示清单；空项目删除清理项目级清单，
  但保留内部 `knowledge_bases` 记录，防止影响旧 RAG/Agent 的文档数据。
- `tests/test_projects_router.py` 覆盖模板复制、冲突、隔离、版本、越权、删除边界；P04.2
  仍需实现清单项 CRUD、项目版本联动与派生状态。

#### AV1-P04.2 清单项 API、版本控制与派生状态（待开始，FR-031）

**目标与范围**

- 提供 `GET/POST/PATCH/DELETE /projects/{project_id}/checklist-items`，只允许项目所有者访问和维护项目独立清单项。
- `POST` 创建清单项必须校验 `expected_project_version`；在同一事务中创建清单项、写入脱敏审计并使 `projects.version` 原子加一。清单项修改使用其 `expected_version` 防止覆盖新数据；旧版本均返回 `409 VERSION_CONFLICT`。
- 清单列表实时派生 `SATISFIED`、`MISSING`、`NOT_PROVIDED` 和 `confirmed_document_count`。只有同项目 `CONFIRMED` 档案与 `CONFIRMED` 关联同时存在才为 `SATISFIED`；必需项无满足关联为 `MISSING`，可选项无满足关联为 `NOT_PROVIDED`。不得仅按资料类型或项目阶段自动判定满足。
- 修改清单项的名称、资料类型或项目阶段时，使已有确认关联失效并重算状态；仅修改说明不影响关联。删除清单项时删除其关联并写入脱敏审计。
- 本任务只实现清单项维护和读取已有确认关联后的派生结果；不提供档案—清单项关联建议、创建或删除接口，该部分仍由 AV1-P10（FR-037）实现。

**验证重点**

- 模板复制后的项目清单可以独立增删改，跨项目或跨用户不能读取、修改或删除。
- 创建请求的旧项目版本、修改请求的旧清单项版本均返回稳定的 `409 VERSION_CONFLICT`，且不产生重复清单项或审计成功记录。
- 当前尚无已确认关联时，必需项返回 `MISSING`、可选项返回 `NOT_PROVIDED`；后续存在已确认档案和已确认关联时才返回 `SATISFIED`。
- 修改名称、资料类型或项目阶段会使已存在的确认关联失效；只改说明不会失效。删除清单项后不再出现在列表，且其关联被清理。

### AV1-C01 Chroma 独立可行性验证（已完成，2026-08-07）

**目标与范围**

- 在目标 2 vCPU / 2 GB 云环境，以 Chroma 单机服务替代 `etcd + MinIO + Milvus`；Chroma
  只加入内部网络和持久化卷，不映射宿主机端口。
- 使用虚构资料验证 Chroma 内网心跳、容器重启持久化、`user_id + kb_id` 隔离和精确删除；
  记录 Chroma 空闲内存与主机可用内存，不得删除或修改
  现有 Milvus 数据。

**验证重点**

- 已在目标环境通过内部网络、过滤、精确删除、容器重启持久化和空闲资源检查；实测记录见
  `docs/chroma-migration-decision.md`。C01 执行时当前代码仍依赖 Milvus，因此本任务不能验证完整应用栈。
- FastAPI、PostgreSQL、Chroma 与本地 BGE 的完整栈 OOM/重启/健康验证改为 AV1-C02 的
  完成条件；若未通过，要求用户选择增加内存、迁出 PostgreSQL 或拆分 Embedding 服务。
- 仅 FastAPI 对外发布 HTTP(S)；直接访问 Chroma 的宿主机端口必须不可用。

### AV1-C02 Chroma 向量后端迁移（本地验证已完成，2026-08-07）

**目标与范围**

- 移除运行时代码、依赖和 Compose 对 Milvus、etcd、MinIO 的依赖，改用 Chroma HTTP 客户端、
  持久化服务、健康检查和受控错误映射；锁定 Chroma 镜像版本或 digest。
- 重写 `vector_service`、旧制度检索、文档删除和健康检查的向量调用，保持既有 HTTP API、
  `user_id + kb_id + document_id` 服务端范围过滤与脱敏错误契约。
- 不迁移历史向量；仅在 Chroma 测试通过后，由原文件和业务记录重建虚构/可授权资料的向量。
- 用 AV1-P02 验收集重新标定距离度量、分数方向和阈值；不得复用旧 Milvus COSINE 阈值。
- 本机验证 FastAPI、PostgreSQL、Chroma 和本地 BGE 的连通与健康状态；目标云环境的完整栈资源、
  向量化、检索、OOM/重启验证暂定至 V1 完成后的部署决策，不得以 C01 的 Chroma 空闲内存替代。

**验证重点**

- 旧制度检索的写入、检索、引用、隔离和删除回归通过；Chroma 断连只返回稳定
  `VECTOR_UNAVAILABLE`，不泄露内部地址或异常。
- 本机 Chroma 不对局域网或公网暴露，重建过程不产生跨用户/知识库数据泄露；云端完整栈的
  内存、OOM、重启与可恢复健康检查属于后续部署验收，不是当前开发完成条件。

### AV1-P05 项目内上传与重复校验

**目标与范围**

- 实现只接收 PDF/DOCX/TXT/MD 的 multipart 上传；在文件落盘前完成所有权、20 MB、100 份现存文档和同项目 `file_hash` 校验。
- 创建原文件和 `UPLOADED` 归档记录；上传不得自动解析、调用模型或写 Chroma。

**验证重点**

- 同项目重复返回当前项目既有文档摘要且无新文件/记录；不同项目相同文件可上传且不泄露信息。
- 容量并发下以项目行锁/原子计数防止突破 100；超限不产生半成品。

### AV1-P06 正式解析、快照与重试

**目标与范围**

- 将 AV1-P01 冻结的解析规则接入 `UPLOADED → PARSED` 与 `PARSE_FAILED → PARSED` 专用重试流程。
- 保存不可变 `ParsedSnapshot`、`file_hash` 与 `snapshot_hash`，记录受控失败摘要；普通 parse 的错误状态和重复调用均保持幂等。

**验证重点**

- 四格式的定位、扫描 PDF 提示、空文本阈值、解析重试、快照不可替换及旧状态操作拒绝。
- 解析不写正式向量、不生成字段草稿、不递增草稿版本。

### AV1-P07 手工草稿、字段证据与人工检查

**目标与范围**

- 实现 `manual-draft`、草稿读取、七字段更新、证据保存、检查状态、乐观锁，以及已确认数据修改后的 `PENDING_RECONFIRMATION`。
- 先建立不依赖模型的完整人工归档路径：标题和资料类型必须非空，其他字段可明确接受为空；人工无证据值必须显式标记。

**验证重点**

- `PARSED/SUGGESTION_FAILED → PENDING_CONFIRMATION` 的空白草稿入口；未开始草稿不得编辑。
- 七字段仍有待检查、标题/类型为空、版本陈旧、伪造 AI 来源均被拒绝。
- 已确认档案修改字段或证据后立即退出正式范围；此时尚未要求其已实现正式索引。

### AV1-P08 AI 建议与安全重新生成

**目标与范围**

- 实现首次建议、失败后重试、未人工编辑草稿的重新生成；模型只能使用当前文档快照和固定字典。
- 校验 AI 非空字段均有当前快照证据；模型不可用与无效输出使用稳定错误码。

**验证重点**

- 单元/API 测试使用 fake/mock DeepSeek，覆盖成功、连接/超时一次重试、格式错误、无字段依据、手工草稿降级。
- 重新生成在模型调用后再次校验版本与人工编辑状态，不能静默覆盖人工字段或人工证据；失败时保留原草稿。
- 实际 AI 准确率不在本任务宣称达标，留给 AV1-P14 的固定验收集。

### AV1-P09 正式确认、Final Collection 与索引纵向切片

**目标与范围**

- 实现确认、取消确认、重复确认幂等、`ArchiveOperation(INDEX)` 和正式可见性闸门。
- 在独立 `archive_final_chunks` Collection 中写入确认后的原文 Chunk；不复用既有制度检索 Collection。
- 运行门槛 2.1：使用实际 BGE/Chroma 确认向量维度、Collection 距离配置、`confirmed_document_ids ≤ 100` 的 `where` 过滤，以及本地 P95 检索延迟记录。

**验证重点**

- 仅所有字段检查完成且必填字段非空时可确认；成功后写一次审计并进入正式范围。
- 确认失败不能假装成功；取消确认和字段修改均保守移出正式范围并清理/阻断 Final Chunk。
- 2.1 的性能值只记录为本地演示基线，不用实测值反向制造通过阈值。

### AV1-P10 清单关联、处理列表、正式目录与审计

**目标与范围**

- 实现已确认档案与同项目清单项的人工确认关联、关联建议、失效和缺失重算。
- 实现文档处理列表（`documents INNER JOIN archive_documents`，全部未删除状态）与正式档案目录（仅已确认且无可见性阻断）及结构化筛选。
- 实现需求规定的脱敏业务审计查询与受控操作类型筛选。

**验证重点**

- 档案存在但未确认关联时，必需项仍显示缺失；取消确认、删除关联或清单关键字段修改后立即重算。
- 处理列表可见待确认/失败资料；正式目录和详情永不泄露待确认或待重新确认草稿。
- 审计仅返回脱敏摘要，自动重试不重复写成功审计，其他项目审计不可查询。

### AV1-P11 正式检索与相关性阈值标定

**目标与范围**

- 实现 `archive-retrieval`，先从 PostgreSQL 获取正式且可见的 `document_id` 集合，再用服务端注入的 `user_id + project_id + kb_id + document_id` 过滤 Chroma。
- 基于 AV1-P02 固定问题集完成门槛 2.2，冻结 Chroma 距离度量类型、分数方向和 `min_relevance_score`，写入配置与验收说明。

**验证重点**

- 8 个有据问题中至少 7 个结果包含标准证据；2 个无据问题过滤后为空；2 个隔离问题不返回其他项目数据。
- 只返回正式原文 Chunk；待确认、待重新确认、人工无证据字段和删除阻断档案不能出现。

### AV1-P12 带证据问答

**目标与范围**

- 实现 `archive-questions`：复用 AV1-P11 的正式检索范围、固定前 5 个候选和冻结阈值；无依据时不调用 DeepSeek。
- Prompt 和响应只允许基于检索 Chunk，并返回文件名、定位和原文摘录。

**验证重点**

- 模型关闭/不可用返回 `ARCHIVE_ANSWER_UNAVAILABLE`，不影响目录、清单和人工归档。
- 以 12 个固定问题完成门槛 3.2：最终回答引用正确率 100%，无伪造引用、无跨项目或未确认内容。

### AV1-P13 物理删除与跨存储恢复

**目标与范围**

- 实现任意稳定状态发起的物理删除：先创建 `DELETE + RUNNING + visibility_blocking`，再清理 Final Chunk、原文件、快照、字段、证据和清单关联。
- 对确认/删除跨 PostgreSQL、文件系统、Chroma 的部分失败记录 `last_completed_step`，复用确认或 `DELETE` 入口完成可重入恢复。

**验证重点**

- 删除成功后只保留脱敏删除审计；项目容量只在业务记录实际移除后递减。
- 用故障注入覆盖 Chroma 失败、文件删除成功但 PostgreSQL 失败等情形；恢复不得重复写数据、泄露可见性、或重复记录成功审计。
- 达成门槛 3.1 后才能将 FR-036/FR-040 的集成验收标为通过。

### AV1-P14 全链路验收与交付文档

**目标与范围**

- 以虚构资料完成从项目创建、上传、解析、AI/手工草稿、确认、关联、检索、问答、删除的 Swagger 演示。
- 汇总单元、数据库集成、Chroma 集成、API 和端到端测试；将实测结果、环境限制和未验证项写入验收报告、README 与演示文档。

**完成条件**

- 每一项 AC-FR-030～AC-FR-041 都有可追溯测试或明确的不适用/未验证说明。
- 不把历史 Agent 结果、mock 结果或单问题在线结果写成智慧档案 V1 的质量结论。
- 文档明确 Swagger 是 V1 界面、样本/清单为虚构演示规则，且 DeepSeek 可能接收已脱敏片段。

## 5. 测试层次与责任边界

| 层次 | 主要任务 | 不应替代的证据 |
|---|---|---|
| 单元测试 | 哈希、状态机、字段校验、Parser 归一化、提示/响应解析、服务规则 | PostgreSQL/Chroma 的真实约束与检索质量 |
| PostgreSQL 集成测试 | 迁移、唯一约束、版本冲突、所有权、派生缺失、审计 | 文件/Chroma 跨存储恢复 |
| Chroma 集成测试 | Collection、向量维度、过滤、删除、重启持久化、检索阈值标定 | DeepSeek 回答质量 |
| API 测试 | 路由、HTTP 状态、Schema、越权、分页、稳定错误码 | 外部模型实际质量 |
| 端到端/验收 | 固定虚构资料上的业务闭环、门槛 2/3 | 生产级容量、可用性或安全承诺 |

每个任务开始时必须先列出该任务关联的 `FR`、验收标准、具体文件、测试命令和预期结果；
任务结束后只报告实际运行过的命令与结果。未运行的 PostgreSQL、Chroma、BGE 或 DeepSeek 依赖
必须单独标注，不得以静态检查替代。

## 6. 当前状态与下一步

- AV1-P01、AV1-P02、AV1-P03 已完成。P03 已新增归档 SQLModel、`ProjectContext` 所有权依赖与 `0005_archive_v1_schema`～`0008_legacy_business_comments` 前向迁移；目标 PostgreSQL 空库已实际迁移至当前版本，且全部 16 张业务表、143 个字段的注释均可由 PostgreSQL 元数据读取。
- P03 离线迁移 SQL、SQLite 约束测试、完整回归和编译已通过；最新全量回归为 `84 passed, 1 skipped, 16 warnings`。专用 `POSTGRES_TEST_URL` 自动化迁移测试仍未配置，不能替代本次空库实迁结果。
- P04 前置结构调整已完成：归档 SQLModel 已按 `project.py`、`archive.py`、`checklist.py`、`archive_audit.py` 拆分，`app.models` 公共导入入口与表名、字段、约束保持不变；模型元数据、迁移/授权/注释测试和编译均已通过。本调整不新增迁移，也不实现项目或清单 API。
- P04.1 FR-030 已完成。AV1-C01 的历史独立实验和 AV1-C02 的本地代码/单测/命名空间/Docker
  健康验证均已完成；当前运行和开发基线为本地。云端部署与完整栈资源验证暂定至 V1 功能完成后。
  下一步为 P04.2 清单项 API 与派生状态，它不依赖向量库。
- AV1-P02 已解除对 AV1-P11 阈值标定和 AV1-P12 问答质量验收的“验收资料缺失”阻塞；
  这些任务仍分别依赖 P09 正式索引和 P11 正式检索实现。
